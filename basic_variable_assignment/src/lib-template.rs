fn increment_by_one(x: i32) -> i32 {
    // Put Code Here
}

fn increment_mutable(x: &mut i32) -> () {
    // Put Code Here
    // Hint: note the function signature, x is a mutable reference.
    // In order to mutate the value contained in x, you have to dereference it with the * operator,
    // i.e. *x = ...;
}
